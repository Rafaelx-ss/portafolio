{
    "build": {
        "command": "ng build --configuration=production",
        "outputDirectory": "dist"
    },
    "routes": [
    {
        "src": "/(.*)",
        "dest": "/index.html"
    }
]
}
